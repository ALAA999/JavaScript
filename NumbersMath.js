let num = Math.PI; // 3.141592653589793
num = Math.round(2.4); // 2
num = Math.round(2.7); // 3
num = Math.ceil(2.4); // 3
num = Math.sqrt(64); // 8 

num = Math.min(2, 4); // 2
num = Math.max(2, 4, 5, 6); // 6
num = Math.random();
num = Math.floor(Math.random() * 20 + 1);
console.log(num);